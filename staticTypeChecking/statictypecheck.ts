const ob = {
    name: 'thofiq',
    age: 32
}

// console.log(ob.address); //compilation error : address doesn't exist // java script run without error

function randomNumberGenerator(){
    //return Math.random<0.5; // type checking error
}

// console.log(randomNumberGenerator()); //Error: cant apply to function and number // java script run without error
